# Server deploy folder (contents not in Git)

Copy **`visit.php`**, optional **`ping.php`** (sanity check), optional **`ingest.php`** (second copy of `visit.php`), **`.htaccess`**, and server-only **`config.php`** here for upload to `noyanov.com/Apps/data/api/`. Do not `git add` files in this directory except this README.

Templates (no secrets): `../templates/php/`
