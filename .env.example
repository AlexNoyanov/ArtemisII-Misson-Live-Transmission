# Copy to ".env" for local development (never commit .env).
#
# Optional: NASA / api.data.gov key for api.nasa.gov. This dashboard does NOT send it to the browser.
# Live Artemis numbers use /api/telemetry (no key). Set the key only if you add server routes that call api.nasa.gov.
NASA_API_KEY=
